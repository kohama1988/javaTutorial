package Basic;
public class Accessability {
    public static void main(String[] args) {
        //private   只能在类内部访问
        //缺省        可在类内部，同一个包中访问 
        //protected     可在类内部，同一个包中，不同包的子类中访问
        //public        可在类内部，同一个包中，不同包的子类中，同一个工程中访问
        /*
        对于class的权限修饰只可用public和default缺省
        public可以在任意地方被访问
        default缺省只可在被同一个包内部的类访问
        */
    }
}
